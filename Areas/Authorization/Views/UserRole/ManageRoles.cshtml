@* @{
    ViewData["Title"] = "ManageRoles";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers
@addTagHelper *, AuthoringTagHelpers
@model FleetMasterPro013.Areas.Authorization.Models.ManageUserRolesViewModel

<h1>Manage Roles for @Model.UserName</h1>

<form asp-action="ManageRoles" method="post">
    <input type="hidden" asp-for="UserId" />
    <input type="hidden" asp-for="UserName" />

    <table class="table table-bordered">
        <thead>
            <tr>
                <th>Role</th>
                <th>Assign</th>
            </tr>
        </thead>
        <tbody>
            @for (int i = 0; i < Model.Roles.Count; i++)
            {
                <tr>
                    <td>
                        @Model.Roles[i].RoleName
                        <input type="hidden" asp-for="Roles[i].RoleName" />
                    </td>
                    <td>
                        <input asp-for="Roles[i].IsSelected" type="checkbox" class="form-check-input" />
                    </td>
                </tr>
            }
        </tbody>
    </table>

    <button type="submit" class="btn btn-primary">Save Changes</button>
    <a asp-action="Index" class="btn btn-secondary">Cancel</a>
</form>
 *@

@{
    ViewData["Title"] = "Manage Roles";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers
@addTagHelper *, AuthoringTagHelpers
@model FleetMasterPro013.Areas.Authorization.Models.ManageUserRolesViewModel

<div class="container mt-5">
    <div class="card shadow-sm">
        <div class="card-header bg-primary text-white">
            <h3 class="mb-0">Manage Roles for <strong>@Model.UserName</strong></h3>
        </div>
        <div class="card-body">
            <form asp-action="ManageRoles" method="post">
                <input type="hidden" asp-for="UserId" />
                <input type="hidden" asp-for="UserName" />

                <div class="table-responsive">
                    <table class="table table-bordered table-striped align-middle">
                        <thead class="table-light">
                            <tr>
                                <th>Role</th>
                                <th>Assign</th>
                            </tr>
                        </thead>
                        <tbody>
                            @for (int i = 0; i < Model.Roles.Count; i++)
                            {
                                <tr>
                                    <td>
                                        @Model.Roles[i].RoleName
                                        <input type="hidden" asp-for="Roles[i].RoleName" />
                                    </td>
                                    <td class="text-center">
                                        <input asp-for="Roles[i].IsSelected" type="checkbox" class="form-check-input" />
                                    </td>
                                </tr>
                            }
                        </tbody>
                    </table>
                </div>

                <div class="d-flex justify-content-between mt-4">
                    <button type="submit" class="btn btn-primary">
                        <i class="fas fa-save"></i> Save Changes
                    </button>
                    <a asp-action="Index" class="btn btn-secondary">
                        <i class="fas fa-arrow-left"></i> Cancel
                    </a>
                </div>
            </form>
        </div>
    </div>
</div>
